# 本地配置（含数据库密码，绝不能进镜像/构建上下文）
config.yaml

# 运行时数据（用宿主机卷挂载，不需要打进镜像）
logs/
uploads/
dist/

# 本地文档
docs/
ui-interaction-effects.md

# 前端依赖/构建缓存（Dockerfile 内重新 npm ci）
web/node_modules/

# 版本控制 / IDE
.git
.gitignore
.idea/
.vscode/
*.iml

# 编译产物
*.exe
*.out
blog-system
